{
  "private": true,
  "name": "NorthpolerConfig",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "preact build",
    "serve": "sirv build --port 8080 --cors --single",
    "dev": "preact watch",
    "lint": "eslint src",
    "deploy": "cp build/{index.html,manifest.json,bundle*js,polyfill*js} ../data"
  },
  "eslintConfig": {
    "extends": "eslint-config-synacor"
  },
  "eslintIgnore": [
    "build/*"
  ],
  "devDependencies": {
    "eslint": "^6.0.1",
    "eslint-config-synacor": "^3.0.4",
    "preact-cli": "^3.0.0",
    "prettier": "^2.0.5",
    "sirv-cli": "^1.0.3"
  },
  "dependencies": {
    "emotion": "^10.0.27",
    "preact": "^10.1.0",
    "preact-render-to-string": "^5.1.2",
    "wretch": "^1.7.2"
  }
}
